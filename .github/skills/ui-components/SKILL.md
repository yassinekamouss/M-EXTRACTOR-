---
name: ui-components
description: Generates vanilla HTML/CSS cards for metadata categories.
---
# Instructions
1. Create a `<div>` with the class `meta-card`.
2. Use a semantic `<header>` for the category name.
3. Map JSON key-value pairs to a `<ul>` list.
4. Ensure the styles use the project's CSS variables.